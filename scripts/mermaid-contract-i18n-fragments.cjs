/**
 * 自动生成：node scripts/gen-mermaid-contract-fragments.cjs
 * 供 scripts/graphic-templates-i18n.cjs require
 */
'use strict';

module.exports.MERMAID_CONTRACT_EN = [
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x (same as this extension’s bundled renderer)\n- **diagramType**: flowchart\n- **allowedFirstLines**: (after stripping ```mermaid fences and leading spaces, the first non-empty non-`%%` line MUST start with one of these (case-insensitive)): `flowchart TD` | `flowchart TB` | `flowchart BT` | `flowchart RL` | `flowchart LR` | `graph TD` | `graph LR` (legacy aliases)\n- **forbiddenPatterns**: using CJK/spaces/special chars as raw node IDs; unbalanced `[` `]` / `{` `}`; first line must not be `sequenceDiagram` / `gantt` or other diagram kinds as the first line.\n- **minimalExample**: (replace with your topic and nodes; node IDs ASCII only):\n```mermaid\nflowchart TD\n  start([开始]) --> check{是否通过?}\n  check -->|是| step1[执行步骤]\n  check -->|否| end1([结束])\n  step1 --> end2([结束])\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: bpmn_20 (primary deliverable is BPMN 2.0 XML or structured text, not Mermaid)\n- **allowedFirstLines** (only when you also attach a renderable summary diagram): `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: invalid first line inside a ```mermaid` block; using `@startuml` inside Mermaid.\n- **minimalExample** (optional appendix; primary output remains BPMN):\n```mermaid\nflowchart LR\n  start((开始)) --> task[用户任务]\n  task --> gw{网关}\n  gw -->|是| sys[系统任务]\n  sys --> endn((结束))\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: stateDiagram\n- **allowedFirstLines**: `stateDiagram-v2` | `stateDiagram`\n- **forbiddenPatterns**: first line is `flowchart`/`graph`; unescaped special chars in state/event names break parsing.\n- **minimalExample**:\n```mermaid\nstateDiagram-v2\n  [*] --> Idle\n  Idle --> Active: 触发条件\n  Active --> Idle: 复位\n  Active --> [*]: 完成\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_activity_style (when rendering inside this extension, express fork/join/parallelism with flowchart)\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: mixing PlantUML (`@startuml`) inside ```mermaid`; first line is not a flowchart.\n- **minimalExample**: (parallelism via multiple arrows; you may split subgraphs further):\n```mermaid\nflowchart TD\n  A[开始] --> B{分支?}\n  B -->|路径1| C[并行支路1]\n  B -->|路径2| D[并行支路2]\n  C --> E[合并]\n  D --> E\n  E --> F[结束]\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_tree (primary output may be a Markdown tree; if Mermaid is used, use flowchart + subgraph)\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: first line is `mindmap` but uses flowchart edges; subgraph titles without quotes when needed.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  subgraph L1[\"第一层\"]\n    n1[\"节点1\"]\n  end\n  subgraph L2[\"第二层\"]\n    n2[\"子节点\"]\n  end\n  n1 --> n2\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: mindmap\n- **allowedFirstLines**: `mindmap`\n- **forbiddenPatterns**: first line is `flowchart`; using `-->` edges inside mindmap (mindmap uses indentation, not arrow edges).\n- **minimalExample**:\n```mermaid\nmindmap\n  root((中心主题))\n    分支1\n      关键词A\n    分支2\n      关键词B\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_org\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: node IDs contain CJK or spaces; missing reporting-line arrows.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  CEO[\"总经理\"]\n  CEO --> VP1[\"VP 销售\"]\n  CEO --> VP2[\"VP 研发\"]\n  VP2 --> M1[\"经理 A\"]\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: timeline\n- **allowedFirstLines**: `timeline`\n- **forbiddenPatterns**: first line is `gantt`; mixing flowchart arrow syntax.\n- **minimalExample**:\n```mermaid\ntimeline\n    title 主题示例\n    section 阶段一\n        事件A : 描述A\n    section 阶段二\n        事件B : 描述B\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: gantt\n- **allowedFirstLines**: `gantt`\n- **forbiddenPatterns**: missing `dateFormat` and `title` (recommended); malformed task lines; first line is `flowchart`.\n- **minimalExample**:\n```mermaid\ngantt\ntitle 项目示例\ndateFormat YYYY-MM-DD\nsection 阶段1\n  任务A :a1, 2026-01-01, 7d\n  任务B :after a1, 5d\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_decision_tree\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: decision diamonds without ≥2 outgoing edges; leaves without a conclusion label.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  Q{条件成立?}\n  Q -->|是| L1[结论A]\n  Q -->|否| Q2{第二条件?}\n  Q2 -->|是| L2[结论B]\n  Q2 -->|否| L3[结论C]\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x (ishikawa needs ≥11.12; matches this project)\n- **diagramType**: ishikawa\n- **allowedFirstLines**: `ishikawa`\n- **forbiddenPatterns**: first line is `flowchart`; using `fishbone` as the first line (must be `ishikawa`); YAML-style cause lists.\n- **minimalExample**: (first line is the problem; following indentation encodes categories/causes):\n```mermaid\nishikawa\n  问题示例:交付延迟\n    人\n      培训不足\n      人手不足\n    流程\n      审批链过长\n      需求变更频繁\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_dfd\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: node IDs contain non-ASCII; missing a closed ```mermaid` fence.\n- **minimalExample**:\n```mermaid\nflowchart LR\n  S[\"数据源\"] --> P1[\"处理1\"]\n  P1 --> D[(数据存储)]\n  P1 --> P2[\"处理2\"]\n  P2 --> T[\"外部目标\"]\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_architecture\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: component IDs in CJK; missing layers or links.\n- **minimalExample**:\n```mermaid\nflowchart TB\n  subgraph client[\"前端\"]\n    UI[\"Web UI\"]\n  end\n  subgraph srv[\"后端\"]\n    API[\"API 网关\"]\n    SVC[\"业务服务\"]\n  end\n  subgraph data[\"数据层\"]\n    DB[(数据库)]\n  end\n  UI --> API --> SVC --> DB\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: sequenceDiagram\n- **allowedFirstLines**: `sequenceDiagram`\n- **forbiddenPatterns**: participant aliases with unescaped parentheses; messages missing colons; wrong first line `flowchart`.\n- **minimalExample**:\n```mermaid\nsequenceDiagram\n  participant U as 用户\n  participant S as 服务\n  U->>S: 请求\n  S-->>U: 响应\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: swot_markdown_primary (primary deliverable is a table; optional Mermaid quadrant summary)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `flowchart TB` | `flowchart LR`\n- **forbiddenPatterns**: non-table output posing as SWOT; Mermaid without four quadrants.\n- **minimalExample** (optional):\n```mermaid\nflowchart TB\n  subgraph S[\"Strength 优势\"]\n    s1[要点]\n  end\n  subgraph W[\"Weakness 劣势\"]\n    w1[要点]\n  end\n  subgraph O[\"Opportunity 机会\"]\n    o1[要点]\n  end\n  subgraph T[\"Threat 威胁\"]\n    t1[要点]\n  end\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: comparison_markdown_primary (primary deliverable is a Markdown table; optional comparison summary as flowchart)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `flowchart LR` | `flowchart TB`\n- **forbiddenPatterns**: turning a score matrix into radar or gantt by mistake.\n- **minimalExample** (optional):\n```mermaid\nflowchart LR\n  subgraph A[\"方案 A\"]\n    a1[维度1:高]\n  end\n  subgraph B[\"方案 B\"]\n    b1[维度1:中]\n  end\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x (radar-beta / curve syntax)\n- **diagramType**: radar-beta\n- **allowedFirstLines**: `radar-beta`\n- **forbiddenPatterns**: first line is bare `radar`; YAML `\"name\": [1,2,3]`; missing `axis` or `curve`; `curve` value count mismatches axes.\n- **minimalExample**:\n```mermaid\nradar-beta\n  title 示例\n  axis A[\"轴A\"], B[\"轴B\"], C[\"轴C\"]\n  curve s1[\"系列1\"]{1, 2, 3}\n  curve s2[\"系列2\"]{3, 2, 1}\n  max 5\n  min 0\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_capability_map (layered structure uses flowchart + subgraph)\n- **allowedFirstLines**: `flowchart TB` | `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: only bullet lists when a renderable diagram is needed; subgraphs without titles.\n- **minimalExample**:\n```mermaid\nflowchart TB\n  subgraph core[\"核心能力\"]\n    c1[\"能力A\"]\n    c2[\"能力B\"]\n  end\n  subgraph sup[\"支撑能力\"]\n    s1[\"支撑1\"]\n    s2[\"支撑2\"]\n  end\n  c1 --> s1\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: journey_markdown_primary (primary deliverable is a table (stage / action / emotion / pain); optional timeline or flowchart summary)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `timeline` | `flowchart LR`\n- **forbiddenPatterns**: mixing timeline and flowchart syntax on one line.\n- **minimalExample** (optional timeline):\n```mermaid\ntimeline\n    title 用户旅程摘要\n    section 认知\n        发现产品 : 好奇\n    section 使用\n        完成关键任务 : 满意或受挫\n```\n",
  "\n\n---\n## Mermaid render contract (TalkWebSour)\n- **Target runtime**: Mermaid 11.x\n- **diagramType**: flowchart_vsm(steps + duration + VA/NVA labels)\n- **allowedFirstLines**: `flowchart LR` | `flowchart TD`\n- **forbiddenPatterns**: no ordered steps; missing VA/NVA labels.\n- **minimalExample**:\n```mermaid\nflowchart LR\n  s1[\"步骤1\\n2h VA\"] --> s2[\"步骤2\\n1h NVA\"]\n  s2 --> s3[\"步骤3\\n3h VA\"]\n```\n"
];

module.exports.MERMAID_CONTRACT_KO = [
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x (same as this extension’s bundled renderer)\n- **diagramType**: flowchart\n- **allowedFirstLines**: (after stripping ```mermaid fences and leading spaces, the first non-empty non-`%%` line MUST start with one of these (case-insensitive)): `flowchart TD` | `flowchart TB` | `flowchart BT` | `flowchart RL` | `flowchart LR` | `graph TD` | `graph LR` (legacy aliases)\n- **forbiddenPatterns**: using CJK/spaces/special chars as raw node IDs; unbalanced `[` `]` / `{` `}`; first line must not be `sequenceDiagram` / `gantt` or other diagram kinds as the first line.\n- **minimalExample**: (replace with your topic and nodes; node IDs ASCII only):\n```mermaid\nflowchart TD\n  start([开始]) --> check{是否通过?}\n  check -->|是| step1[执行步骤]\n  check -->|否| end1([结束])\n  step1 --> end2([结束])\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: bpmn_20 (primary deliverable is BPMN 2.0 XML or structured text, not Mermaid)\n- **allowedFirstLines** (only when you also attach a renderable summary diagram): `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: invalid first line inside a ```mermaid` block; using `@startuml` inside Mermaid.\n- **minimalExample** (optional appendix; primary output remains BPMN):\n```mermaid\nflowchart LR\n  start((开始)) --> task[用户任务]\n  task --> gw{网关}\n  gw -->|是| sys[系统任务]\n  sys --> endn((结束))\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: stateDiagram\n- **allowedFirstLines**: `stateDiagram-v2` | `stateDiagram`\n- **forbiddenPatterns**: first line is `flowchart`/`graph`; unescaped special chars in state/event names break parsing.\n- **minimalExample**:\n```mermaid\nstateDiagram-v2\n  [*] --> Idle\n  Idle --> Active: 触发条件\n  Active --> Idle: 复位\n  Active --> [*]: 完成\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_activity_style (when rendering inside this extension, express fork/join/parallelism with flowchart)\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: mixing PlantUML (`@startuml`) inside ```mermaid`; first line is not a flowchart.\n- **minimalExample**: (parallelism via multiple arrows; you may split subgraphs further):\n```mermaid\nflowchart TD\n  A[开始] --> B{分支?}\n  B -->|路径1| C[并行支路1]\n  B -->|路径2| D[并行支路2]\n  C --> E[合并]\n  D --> E\n  E --> F[结束]\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_tree (primary output may be a Markdown tree; if Mermaid is used, use flowchart + subgraph)\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: first line is `mindmap` but uses flowchart edges; subgraph titles without quotes when needed.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  subgraph L1[\"第一层\"]\n    n1[\"节点1\"]\n  end\n  subgraph L2[\"第二层\"]\n    n2[\"子节点\"]\n  end\n  n1 --> n2\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: mindmap\n- **allowedFirstLines**: `mindmap`\n- **forbiddenPatterns**: first line is `flowchart`; using `-->` edges inside mindmap (mindmap uses indentation, not arrow edges).\n- **minimalExample**:\n```mermaid\nmindmap\n  root((中心主题))\n    分支1\n      关键词A\n    分支2\n      关键词B\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_org\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: node IDs contain CJK or spaces; missing reporting-line arrows.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  CEO[\"总经理\"]\n  CEO --> VP1[\"VP 销售\"]\n  CEO --> VP2[\"VP 研发\"]\n  VP2 --> M1[\"经理 A\"]\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: timeline\n- **allowedFirstLines**: `timeline`\n- **forbiddenPatterns**: first line is `gantt`; mixing flowchart arrow syntax.\n- **minimalExample**:\n```mermaid\ntimeline\n    title 主题示例\n    section 阶段一\n        事件A : 描述A\n    section 阶段二\n        事件B : 描述B\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: gantt\n- **allowedFirstLines**: `gantt`\n- **forbiddenPatterns**: missing `dateFormat` and `title` (recommended); malformed task lines; first line is `flowchart`.\n- **minimalExample**:\n```mermaid\ngantt\ntitle 项目示例\ndateFormat YYYY-MM-DD\nsection 阶段1\n  任务A :a1, 2026-01-01, 7d\n  任务B :after a1, 5d\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_decision_tree\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: decision diamonds without ≥2 outgoing edges; leaves without a conclusion label.\n- **minimalExample**:\n```mermaid\nflowchart TD\n  Q{条件成立?}\n  Q -->|是| L1[结论A]\n  Q -->|否| Q2{第二条件?}\n  Q2 -->|是| L2[结论B]\n  Q2 -->|否| L3[结论C]\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x (ishikawa needs ≥11.12; matches this project)\n- **diagramType**: ishikawa\n- **allowedFirstLines**: `ishikawa`\n- **forbiddenPatterns**: first line is `flowchart`; using `fishbone` as the first line (must be `ishikawa`); YAML-style cause lists.\n- **minimalExample**: (first line is the problem; following indentation encodes categories/causes):\n```mermaid\nishikawa\n  问题示例:交付延迟\n    人\n      培训不足\n      人手不足\n    流程\n      审批链过长\n      需求变更频繁\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_dfd\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: node IDs contain non-ASCII; missing a closed ```mermaid` fence.\n- **minimalExample**:\n```mermaid\nflowchart LR\n  S[\"数据源\"] --> P1[\"处理1\"]\n  P1 --> D[(数据存储)]\n  P1 --> P2[\"处理2\"]\n  P2 --> T[\"外部目标\"]\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_architecture\n- **allowedFirstLines**: `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: component IDs in CJK; missing layers or links.\n- **minimalExample**:\n```mermaid\nflowchart TB\n  subgraph client[\"前端\"]\n    UI[\"Web UI\"]\n  end\n  subgraph srv[\"后端\"]\n    API[\"API 网关\"]\n    SVC[\"业务服务\"]\n  end\n  subgraph data[\"数据层\"]\n    DB[(数据库)]\n  end\n  UI --> API --> SVC --> DB\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: sequenceDiagram\n- **allowedFirstLines**: `sequenceDiagram`\n- **forbiddenPatterns**: participant aliases with unescaped parentheses; messages missing colons; wrong first line `flowchart`.\n- **minimalExample**:\n```mermaid\nsequenceDiagram\n  participant U as 用户\n  participant S as 服务\n  U->>S: 请求\n  S-->>U: 响应\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: swot_markdown_primary (primary deliverable is a table; optional Mermaid quadrant summary)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `flowchart TB` | `flowchart LR`\n- **forbiddenPatterns**: non-table output posing as SWOT; Mermaid without four quadrants.\n- **minimalExample** (optional):\n```mermaid\nflowchart TB\n  subgraph S[\"Strength 优势\"]\n    s1[要点]\n  end\n  subgraph W[\"Weakness 劣势\"]\n    w1[要点]\n  end\n  subgraph O[\"Opportunity 机会\"]\n    o1[要点]\n  end\n  subgraph T[\"Threat 威胁\"]\n    t1[要点]\n  end\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: comparison_markdown_primary (primary deliverable is a Markdown table; optional comparison summary as flowchart)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `flowchart LR` | `flowchart TB`\n- **forbiddenPatterns**: turning a score matrix into radar or gantt by mistake.\n- **minimalExample** (optional):\n```mermaid\nflowchart LR\n  subgraph A[\"方案 A\"]\n    a1[维度1:高]\n  end\n  subgraph B[\"方案 B\"]\n    b1[维度1:中]\n  end\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x (radar-beta / curve syntax)\n- **diagramType**: radar-beta\n- **allowedFirstLines**: `radar-beta`\n- **forbiddenPatterns**: first line is bare `radar`; YAML `\"name\": [1,2,3]`; missing `axis` or `curve`; `curve` value count mismatches axes.\n- **minimalExample**:\n```mermaid\nradar-beta\n  title 示例\n  axis A[\"轴A\"], B[\"轴B\"], C[\"轴C\"]\n  curve s1[\"系列1\"]{1, 2, 3}\n  curve s2[\"系列2\"]{3, 2, 1}\n  max 5\n  min 0\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_capability_map (layered structure uses flowchart + subgraph)\n- **allowedFirstLines**: `flowchart TB` | `flowchart TD` | `flowchart LR`\n- **forbiddenPatterns**: only bullet lists when a renderable diagram is needed; subgraphs without titles.\n- **minimalExample**:\n```mermaid\nflowchart TB\n  subgraph core[\"核心能力\"]\n    c1[\"能力A\"]\n    c2[\"能力B\"]\n  end\n  subgraph sup[\"支撑能力\"]\n    s1[\"支撑1\"]\n    s2[\"支撑2\"]\n  end\n  c1 --> s1\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: journey_markdown_primary (primary deliverable is a table (stage / action / emotion / pain); optional timeline or flowchart summary)\n- **allowedFirstLines** (only when a Mermaid appendix is included): `timeline` | `flowchart LR`\n- **forbiddenPatterns**: mixing timeline and flowchart syntax on one line.\n- **minimalExample** (optional timeline):\n```mermaid\ntimeline\n    title 用户旅程摘要\n    section 认知\n        发现产品 : 好奇\n    section 使用\n        完成关键任务 : 满意或受挫\n```\n",
  "\n\n---\n## Mermaid 렌더링 규약 (TalkWebSour)\n- **대상 런타임**: Mermaid 11.x\n- **diagramType**: flowchart_vsm(steps + duration + VA/NVA labels)\n- **allowedFirstLines**: `flowchart LR` | `flowchart TD`\n- **forbiddenPatterns**: no ordered steps; missing VA/NVA labels.\n- **minimalExample**:\n```mermaid\nflowchart LR\n  s1[\"步骤1\\n2h VA\"] --> s2[\"步骤2\\n1h NVA\"]\n  s2 --> s3[\"步骤3\\n3h VA\"]\n```\n"
];
