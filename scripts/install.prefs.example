# 复制本文件为 scripts/install.prefs 后生效（建议加入 .gitignore）
#
# 发行包已自带 agency 数据；一键脚本默认只做 npm install。
# 设为 1 时：在 npm install 之后自动执行 npm run build:agency（你修改了 third_party/agency-agents 时用）
BUILD_AGENCY_AFTER_INSTALL=0
